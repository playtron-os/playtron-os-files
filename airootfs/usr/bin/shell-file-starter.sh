chmod +x -R clatd-ipv6-check
chmod +x -R create-swap.sh
chmod +x -R hwctl
chmod +x -R playtron-factory-reset
chmod +x -R playtronos-update
chmod +x -R resize-root-file-system.sh
chmod +x -R /etc/playstall/install.sh
